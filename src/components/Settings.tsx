import * as React from 'react';

const Settings = (): JSX.Element => (
  <div className="settings panel">
    <span className="type type--pos-large-normal">Work in progress</span>
  </div>
);

export default Settings;
